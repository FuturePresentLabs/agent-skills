# FPL Agent Skills

Monorepo for Future Present Labs agent skills.

## Skills

- `skills/find-stl` — Search + fetch downloadable 3D models from Printables and write a manifest for quoting/printing.

## Development

Each skill is a self-contained folder with `SKILL.md` plus optional `scripts/` and `references/`.
